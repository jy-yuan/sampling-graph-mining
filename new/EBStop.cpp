#pragma once
#include "EBStop.hpp"

void EBStop::init() {
    memset(ebexi, 0, 100*sizeof(bool));
    memset(ebarr, 0, 100*sizeof(int));
}

int EBStop::add(int rank, int x) {
    return 0;
}

void print_res() {

}